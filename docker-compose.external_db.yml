# Copyright (C) CVAT.ai Corporation
#
# SPDX-License-Identifier: MIT

# This optional Docker Compose file may be used to deploy EAIP Annotator with an external database.

x-backend-settings: &backend-settings
  environment:
    CVAT_POSTGRES_HOST:
    CVAT_POSTGRES_PORT:
    CVAT_POSTGRES_DBNAME:
    CVAT_POSTGRES_USER:
    CVAT_POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
  secrets:
    - postgres_password

services:
  eaipannotator_db:
    deploy:
      replicas: 0

  eaipannotator_server: *backend-settings
  eaipannotator_worker_utils: *backend-settings
  eaipannotator_worker_annotation: *backend-settings
  eaipannotator_worker_export: *backend-settings
  eaipannotator_worker_import: *backend-settings
  eaipannotator_worker_quality_reports: *backend-settings
  eaipannotator_worker_webhooks: *backend-settings
  eaipannotator_worker_chunks: *backend-settings
  eaipannotator_worker_consensus: *backend-settings

secrets:
  postgres_password:
    environment: CVAT_POSTGRES_PASSWORD
